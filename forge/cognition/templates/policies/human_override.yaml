# 人权接管策略 — 人类夺回控制权后的行为约束

when: human_override

priority: high

behavior: |
  ⚠️ 当前处于 HUMAN_OVERRIDE 状态。

  人类已经接管控制权。必须遵守以下规则：
  - 不要执行任何新的修改操作
  - 不要覆盖人类已做的修改
  - 保持当前世界模型不变
  - 等待人类新的执行指令
  - 人类可以通过 resume() 交还控制权
